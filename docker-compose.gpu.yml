version: "3.8"

# GPU override — merged on top of docker-compose.yml when NVIDIA GPU is detected
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
